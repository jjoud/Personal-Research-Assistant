# MCP Notes

The Model Context Protocol is useful when an AI application needs reusable access
to tools or data sources. In this project, filesystem access is exposed through a
small MCP-style server so agents do not write files directly.
